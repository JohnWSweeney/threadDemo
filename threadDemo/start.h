#pragma once
#include <iostream>
#include <vector>
#include <sstream>
#include <Windows.h>

void getInput(std::vector<std::string> &tokens);
void startMenu(bool &running, std::vector<std::string> &tokens);